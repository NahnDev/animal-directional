import { CSSProperties } from "react";
import { colors } from "../constants/colors";

export const borderRadiusStyle: CSSProperties = {
    borderRadius: 10,
};

export const borderStyle: CSSProperties = {
    border: `solid .2em ${colors.dark} `,
};
